---
title: Badge
description: Badges highlight important information with small, prominent labels. They indicate status, counts, categories, or draw attention to specific content.
sidebar_position: 10
---

import ComponentShowcase from '@site/src/components/ComponentShowcase';
import ComponentChangelog from '@site/src/components/ComponentChangelog';
import TocLinks from '@site/src/components/TocLinks';

# Badge

Badges are small visual labels that help users quickly understand status and categories. The ELEVATE Badge component helps designers and developers create consistent, accessible interfaces that communicate important information without overwhelming the user.

<TocLinks />

## Overview

### When to Use Badges
- **Status indication**: Online/offline, active/inactive, approval states
- **Categorization**: Tags, labels, content classification
- **Metadata**: Version numbers, types, feature flags
- **Highlighting**: Important or featured content that needs attention

### When NOT to Use Badges
- **Primary actions**: Use Button component for main interactions
- **Navigation**: Use Link or navigation components for page transitions
- **Notification counts**: Use Indicator component for unread counts, new items, alerts
- **Large text content**: Use appropriate text components for substantial information
- **Complex information**: Use Cards or other layout components for detailed data

## Variations

### Status Badges

Badges indicating various status states with semantic colors.

<ComponentShowcase
  title="Status Badges"
  description="Status badges with semantic colors for different states."
  code={`<elvt-stack direction="row" gap="s" wrap
  code="status-badges.html">
  <elvt-badge tone="success">Active</elvt-badge>
  <elvt-badge tone="warning">Pending</elvt-badge>
  <elvt-badge tone="danger">Inactive</elvt-badge>
  <elvt-badge tone="primary">Draft</elvt-badge>
  <elvt-badge tone="neutral">Archived</elvt-badge>
</elvt-stack>`}
>
  <elvt-stack direction="row" gap="s" wrap>
    <elvt-badge tone="success">Active</elvt-badge>
    <elvt-badge tone="warning">Pending</elvt-badge>
    <elvt-badge tone="danger">Inactive</elvt-badge>
    <elvt-badge tone="primary">Draft</elvt-badge>
    <elvt-badge tone="neutral">Archived</elvt-badge>
  </elvt-stack>
</ComponentShowcase>


## Design Principles

### Clarity Principle
**Badge content should be immediately understandable.** Users should be able to quickly interpret badge meaning without requiring additional context or explanation.

### Hierarchy Principle
**Badges should support, not compete with, primary content.** Badge styling should draw appropriate attention without overwhelming the main interface elements.

### Consistency Principle
**Similar information should use similar badge treatments.** Status badges, count badges, and category badges should maintain consistent styling patterns across the application.

### Accessibility Principle
**All users must be able to perceive and understand badge information.** This includes proper color contrast, meaningful text, and support for assistive technologies.

## Best Practices

### Using Semantic Colors

**Why this matters:** Color carries meaning and helps users quickly understand information. Consistent semantic color usage creates predictable user experiences.

:::tip Do: Use semantic tones that match user expectations
Apply tones that align with common conventions and user mental models.

```html
<elvt-badge tone="success">Approved</elvt-badge>
<elvt-badge tone="warning">Under Review</elvt-badge>
<elvt-badge tone="danger">Rejected</elvt-badge>
<elvt-badge tone="primary">Draft</elvt-badge>
```
:::

:::danger Don't: Use arbitrary tones without meaning
Avoid tone choices that confuse users or contradict established patterns.

```html
<!-- Avoid this -->
<elvt-badge tone="success">Error</elvt-badge>
<elvt-badge tone="danger">Success</elvt-badge>
```
:::

## Implementation

### Getting Started

```bash
npm install @inform-elevate/elevate-core-ui
```

### Basic Usage

```html
<!-- Status badges -->
<elvt-badge tone="success">Active</elvt-badge>
<elvt-badge tone="warning">Pending</elvt-badge>
<elvt-badge tone="danger">Error</elvt-badge>

<!-- Label badges -->
<elvt-badge tone="primary">New</elvt-badge>
<elvt-badge tone="neutral">Tag</elvt-badge>
<elvt-badge tone="neutral">Category</elvt-badge>
```

### Key Properties

| Property | Type | Description |
|----------|------|-------------|
| `tone` | `success`, `warning`, `danger`, `primary`, `neutral` | Badge color tone |
| `shape` | `pill` (default) | Badge shape style |
| `padding` | `Padding` or `string` | Custom padding (optional) |
| `pulse` | `boolean` | Adds pulsing animation effect |

*For complete API reference including all properties, events, and methods, see the [Storybook documentation](https://elevate-core-ui.inform-cloud.io/?path=/docs/components-badge--docs).*

## Accessibility

### Essential Accessibility Practices

**Color and contrast:**
- Ensure sufficient color contrast for all text (4.5:1 minimum)
- Don't rely solely on color to convey meaning
- Provide alternative indicators like icons or text
- Test badges in high contrast mode

**Screen reader support:**
- Use appropriate ARIA labels for context
- Provide meaningful text for count badges ("3 unread messages")
- Ensure badge purpose is clear from content alone
- Include status information in labels when needed

## Changelog

<ComponentChangelog 
  component="elvt-badge" 
  maxVersions={10}
  showMetadata={true}
  compactMode={false}
/>