---
title: Icon
description: Icons provide visual communication through recognizable symbols. They enhance usability by communicating concepts quickly and supporting text-based information.
sidebar_position: 11
---

import ComponentShowcase from '@site/src/components/ComponentShowcase';
import ComponentChangelog from '@site/src/components/ComponentChangelog';
import TocLinks from '@site/src/components/TocLinks';

# Icon

Icons create visual consistency and improve interface comprehension through universal symbols. The ELEVATE Icon component helps designers and developers create consistent, accessible interfaces that communicate effectively across language and cultural barriers.

<TocLinks />

## Overview

### When to Use Icons
- **Action identification**: Buttons, menu items, and interactive elements
- **Status indication**: Success, warning, error, and information states
- **Navigation aids**: Wayfinding and section identification
- **Content categorization**: Document types, content themes, and classifications
- **Visual reinforcement**: Supporting text with recognizable symbols

### When NOT to Use Icons
- **Unclear concepts**: Avoid icons for abstract or ambiguous ideas
- **Cultural specificity**: Don't use icons that may not translate across cultures
- **Decoration only**: Icons should serve functional purposes, not just visual appeal
- **Overcrowding**: Too many icons create visual noise and reduce effectiveness

## ELEVATE Custom Icons

The ELEVATE design system includes custom-designed icons that follow our specific design principles and brand guidelines. These icons are optimized for clarity, consistency, and accessibility.

### Available ELEVATE Icons

<ComponentShowcase
  title="ELEVATE Custom Icon Set"
  description="Complete collection of ELEVATE custom icons, designed specifically for the design system"
  code="elevate-custom-icons.html" />

### Usage

ELEVATE custom icons use **no prefix** in the icon attribute:

```html
<!-- ELEVATE custom icons -->
<elvt-icon icon="home"></elvt-icon>
<elvt-icon icon="check"></elvt-icon>
<elvt-icon icon="cancel"></elvt-icon>
<elvt-icon icon="chevron-right"></elvt-icon>
```

## Design Principles

### Recognition Principle
**Icons should be immediately recognizable and universally understood.** Users should not have to guess what icons represent. Unclear or overly stylized icons create cognitive load and slow down task completion.

### Consistency Principle  
**Similar functions should use similar icons throughout the interface.** Inconsistent icon usage confuses users and reduces the efficiency benefits that icons provide.

### Contextual Relevance Principle
**Icons should enhance rather than replace clear communication.** Icons work best when they support text or have established conventions, not when they carry full communication responsibility.

### Visual Hierarchy Principle
**Icon size and treatment should reflect importance and interaction priority.** Primary actions might use larger or more prominent icons while secondary functions use smaller, subtler treatments.

### Accessibility Principle
**All icons must be accessible to users with disabilities.** This includes proper contrast ratios, alternative text, and support for assistive technologies.

### Scalability Principle
**Icons should maintain clarity and legibility at all sizes.** From 16px mobile icons to large display elements, icons must remain recognizable and functional.

## Material Design Icons (MDI)

The ELEVATE Icon component supports the complete Material Design Icons library with 7,447+ icons available. These provide extensive coverage for most interface needs.

### Usage

MDI icons use the **`mdi:` prefix** in the icon attribute:

```html
<!-- Material Design Icons -->
<elvt-icon icon="mdi:home"></elvt-icon>
<elvt-icon icon="mdi:settings"></elvt-icon>
<elvt-icon icon="mdi:chevron-right"></elvt-icon>
```

### Common MDI Icons

<ComponentShowcase
  title="180 Most Common MDI Icons"
  description="The most frequently used Material Design Icons, organized by category for easy reference"
  code="common-mdi-icons.html" />

### Icon Categories

**Action Icons**: Plus, delete, edit, save, copy, share, download, upload, search, filter  
**Navigation Icons**: Home, back, forward, menu, close, chevron directions, arrows  
**Status Icons**: Check, warning, error, info, success, pending, loading  
**System Icons**: Settings, help, notifications, user, security, visibility  
**Content Icons**: File types, media controls, text formatting, attachments  
**Communication Icons**: Email, phone, chat, video call, social media

## Size Variations

Icons can be sized using CSS font-size property for scalable, responsive design.

<ComponentShowcase
  title="Icon Sizes"
  description="Standard icon sizes for different interface contexts and use cases"
  code="icon-sizes.html" />

## Best Practices

### Choosing Recognizable Icons

**Why this matters:** Unfamiliar icons force users to learn new visual vocabulary, increasing cognitive load. Well-established icon conventions help users understand functionality instantly.

**Guidelines:**
- Use conventional icons for common actions (trash for delete, pencil for edit)
- Prefer ELEVATE custom icons for brand consistency when available
- Test icon recognition with target users
- Provide tooltips or labels for less common icons

### Pairing Icons with Text

**Why this matters:** Icons alone can be ambiguous, especially for complex or abstract concepts. Text labels provide clarity and improve accessibility for all users.

**Implementation:**
```html
<!-- Icon with visible text -->
<elvt-button>
  <elvt-icon slot="prefix" icon="plus"></elvt-icon>
  Add New Item
</elvt-button>

<!-- Icon with tooltip -->
<elvt-icon icon="help" title="Get help with this feature"></elvt-icon>
```

### Maintaining Visual Consistency

**Why this matters:** Mixed icon styles create visual inconsistency that makes interfaces feel unprofessional and harder to scan. Consistent treatment improves overall design cohesion.

**Guidelines:**
- Use consistent sizing within the same interface context
- Maintain similar visual weight and style treatment
- Follow the same interaction patterns (hover, focus, active states)

## Implementation

### Getting Started

```bash
npm install @inform-elevate/elevate-core-ui
```

```javascript
// Import all components
import '@inform-elevate/elevate-core-ui';

// Or import specific components  
import '@inform-elevate/elevate-core-ui/dist/components/icon/icon.js';
```

### Common Usage Patterns

```html
<!-- Basic ELEVATE custom icons -->
<elvt-icon icon="home"></elvt-icon>
<elvt-icon icon="check"></elvt-icon>
<elvt-icon icon="cancel"></elvt-icon>

<!-- Basic Material Design Icons -->
<elvt-icon icon="mdi:settings"></elvt-icon>
<elvt-icon icon="mdi:user"></elvt-icon>
<elvt-icon icon="mdi:notification"></elvt-icon>

<!-- Icons with CSS styling -->
<elvt-icon icon="mdi:check-circle" style="color: #24a148; font-size: 20px;"></elvt-icon>
<elvt-icon icon="mdi:alert-circle" style="color: #f1c21b; font-size: 20px;"></elvt-icon>
<elvt-icon icon="mdi:close-circle" style="color: #da1e28; font-size: 20px;"></elvt-icon>

<!-- Icons in buttons (using slots) -->
<elvt-button tone="primary">
  <elvt-icon slot="prefix" icon="plus"></elvt-icon>
  Add Item
</elvt-button>

<elvt-button>
  Continue
  <elvt-icon slot="suffix" icon="chevron-right"></elvt-icon>
</elvt-button>

<!-- Interactive icons with accessibility -->
<elvt-icon 
  icon="mdi:delete" 
  style="color: #da1e28; font-size: 24px; cursor: pointer;"
  title="Delete item"
  aria-label="Delete this item">
</elvt-icon>
```

### Key Properties

| Property | Options | Description |
|----------|---------|-------------|
| `icon` | Icon name string | Icon identifier (e.g., "home", "mdi:settings") |
| `emblem` | SVG path, encoded JSON, or URL | Secondary icon displayed in lower right corner cutout |
| `fill` | Hexadecimal color | Background color for the icon (e.g., `#ff0000`) |
| `selected` | `true`, `false` (default) | Whether the icon appears in selected state |
| `aspectRatio` | String | Aspect ratio for icon container (default: `1 / 1`) |
| `image` | URL string | Alternative image source for icon |

**Styling:** Icons are sized using CSS `font-size` property. Standard HTML attributes like `title`, `aria-label`, and `aria-hidden` can be used for accessibility.

*For complete API reference including all properties, events, and methods, see the [Storybook documentation](https://elevate-core-ui.inform-cloud.io/?path=/docs/components-icon--docs).*

## Accessibility

### Essential Accessibility Practices

**Provide alternative text:**
```html
<!-- Decorative icons (no screen reader announcement) -->
<elvt-icon icon="mdi:star" aria-hidden="true"></elvt-icon>

<!-- Functional icons (with meaning) -->
<elvt-icon icon="mdi:delete" aria-label="Delete item"></elvt-icon>
<elvt-icon icon="mdi:edit" title="Edit item"></elvt-icon>
```

**Don't rely solely on icons:**
- Always provide text labels or context for important actions
- Use icons to enhance, not replace, clear communication
- Consider that icon meanings may not be universal

**Interactive icon accessibility:**
- Interactive icons need proper focus indicators
- Use appropriate ARIA roles for clickable icons
- Ensure adequate touch target size (44px minimum)
- Provide keyboard interaction support

**Color and contrast:**
- Icons must meet 3:1 contrast ratio minimum
- Don't use color alone to convey meaning
- Ensure icons remain visible in high contrast mode

## Changelog

<ComponentChangelog 
  component="elvt-icon" 
  maxVersions={5}
  showMetadata={true}
  compactMode={false}
/>